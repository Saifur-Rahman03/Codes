#include <stdio.h>
#include <stdlib.h>

int main()
{
    char str[100], str1[100];
    int i,j,len=0,len1=0,k;
    printf("enter string to count\n");
    gets(str);
    len=strlen(str);
    gets(str1);
    len1=strlen(str1);
    for(i=0;i<z;i++)
    {
        if(count==0)
    }
