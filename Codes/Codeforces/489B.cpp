#include <bits/stdc++.h>
using namespace std;

int main()
{
	int n;
	cin >> n;
	vector<int> maleSkills;
	for(int i = 0; i < n; i++) {
		int maleSkill;
		maleSkills.push_back(makeSkill);
	}

	int m;
	cin >> m;
	vector<int> femaleSkills;
	for(int i = 0; i < m; i++) {
		int femaleSkill;
		femaleSkill.push_back(femaleSkill);
	}

	vector<int>::iterator itMale;
	vector<int>::iterator itFemale;

	for(auto itMale = maleSkills.begin(); itMale != maleSkills.end(); )



}
