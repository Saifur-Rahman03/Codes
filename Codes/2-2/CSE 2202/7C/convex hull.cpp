#include <iostream>
#include <vector>
#include <fstream>
#include <stack>
#include <algorithm>

using namespace std;



vector < pair <int, int> > v;
vector <pair <int, int> > sortedV;
vector <float> angle;

int main()
{
	ifstream fin;
	fin.open("random_points.txt");
	int n, i, a, b;
	fin >> n;
	for(i = 0; i < n; i++){
		fin >> a >> b;
		v.push_back(make_pair(a, b));
	}
	for(i = 0; i < n; i++){
		angle[i] =
	}
}

void Scan()
{
	*p = List;
	*p1 = List;

}
